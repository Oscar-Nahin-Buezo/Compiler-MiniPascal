program posneg;
var 
 no : integer;
 si: integer;
begin
  Write('Ingrese un numero:');
  readln(no);
  Write('Ingrese otro numero:');
  readln(si);

  if (no > si) then
   writeln('Positivo')
  else
    if (no < si) then
     writeln('Negativo')
 end.